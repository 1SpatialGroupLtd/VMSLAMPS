C Parameter file to set size of the maximum value an input coordinate
C can be after in has been integerised.
C
	INTEGER*4		MAX_RES_INT       ! have int and real to save
	REAL*4			MAX_RES_REAL	  ! needing to cast them
C
	COMMON /RES/	MAX_RES_INT,MAX_RES_REAL
