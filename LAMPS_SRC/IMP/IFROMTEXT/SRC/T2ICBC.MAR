; Command Table for reading CB  codes in IMP IFROMTEXT program
;
	.IDENT	/28OC87/
	.TITLE	T2ICBC
;
	.LIBRARY /LSL$LIBRARY:LSLMACLIB/
;
	$CMTAB T2ICBC, U
;
	$CMD    FL      ; flags
	$CMD	GT	; graphical type
	$CMD	NR	; number of rows
	$CMD	NC	; number of columns
	$CMD    NA      ; number of attributes
	$CMD	AT	; attributes
	$CMD	HC	; header codes
	$CMD    DA      ; data
;
	$CMEND	T2ICBC
;
	.END
