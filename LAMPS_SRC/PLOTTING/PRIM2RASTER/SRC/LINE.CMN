	LOGICAL	L_CLOSED
	REAL	L_WIDTH
	INTEGER	L_CAP
	INTEGER	L_JOIN
	INTEGER	L_NPTS
	INTEGER	L_IND
	REAL	L_COORD(2,4)
	REAL	L_COORD2(2)
C
	COMMON/LINE/L_CLOSED,L_WIDTH,L_CAP,L_JOIN,L_NPTS,L_IND,
     &			L_COORD,L_COORD2
