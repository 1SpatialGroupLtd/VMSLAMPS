%!PS
%
% LSL Prefix file to request Yellow (Y) CMYK separation for PostScript plotting.
%
% If you have a file MYFILE.PS produced by LSL GKSPOSTSCRIPTSHR.EXE,
% and want to produce four separate plots, one for each printing primary,
% then use by eg PRINT SEPARATION_YELLOW.PS+MYFILE.PS,
% or edit the following line into MYFILE.PS as the first non-comment line.
%
/SEPARATION /Y def
%
% End of LSL prefix file for CMYK separation.
%
