! RTXX.LCM - LITES2 terminal-specific initialisation for remote terminal
! See also terminal-independent file SITEINIT.LCM
! This version is example for minimal dumb LITES2.
!
%disable graphics	! assume just VDU there
%disable table		! no table
%prompt state		! prompt with current state rather than *
