!   -----------OS.LCM------------!   For O.S. contracts.!!   Command-File for selecting!   the menu's etc. for O.S.:!@OSPUCK@OSFCSMEN@BURCMDMEN%FIDUCIAL LAYER 0,11%SETUP OS%FRT OS%SRI OS%TRI OS%ENABLE HEIGHT%ENABLE STATUS%ENABLE SUBS%VERIFY TEXT%VERIFY GROUP%INTERPOLATE MCCONALOGUE%TOL CURGEN 0 0.00576 0.0125%tol cirgen 0 0.0038 0%SET TEX 28%ENABLE BIG%DECLARE INTEGER NEXTFSN%DECLARE CHARACTER NEWTEX%MACRO R  # %FIND#%COPY#%MOVE#%INQUIRE NEWTEX "Replace with ?"#%REPL 'NEWTEX'#%ENDM%MACRO B  # %TOGGLE BIG         # %ENDM%MACRO QT # %TOGGLE QUIET       # %ENDM%MACRO AB # %ABANDON	      # %ENDM%MACRO ABO# %TOL OFF -.7407     # %ENDM%MACRO BEL# %TOL OFF 0.7407     # %ENDM%MACRO PD # %FIND # %DEL PART # %FIND # %END # %ENDM%MACRO USEO # %CRE LAY 17# %SEL ALL # %DESE LAY 17 # %ENDM%MACRO EXI #  %NULL # %ENDM! and set up status line!!%ENABLE STATUS!!!!!!! The End.