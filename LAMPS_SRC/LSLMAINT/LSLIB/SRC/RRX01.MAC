

	.TITLE	RRX01

; RESTORE REGISTERS & EXIT

.GLOBL	RRX,RRXT

RRXT:	MOV	(SP)+,R5		; IN HERE VIA TRAP
RRX:	MOV	(SP)+,R0
	MOV	(SP)+,R1
	MOV	(SP)+,R2
	MOV	(SP)+,R3
	MOV	(SP)+,R4
	RTS	R5

	.END
