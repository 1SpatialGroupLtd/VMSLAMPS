

	.TITLE	SCC03X

;	RSX-11M	 version

; store completion code R2 and exit
; entered by JMP

.GLOBL	SCCOD,SCCOB,BCKSPC
SCCOB:	JSR	PC,BCKSPC	; ENTRY TO GET A BACKSPACE
SCCOD:	CMPB	@R5,#1
	BEQ	1$			; cond code arg not given
	MOV	R2,@4(R5)
1$:	RTS	PC

	.END
