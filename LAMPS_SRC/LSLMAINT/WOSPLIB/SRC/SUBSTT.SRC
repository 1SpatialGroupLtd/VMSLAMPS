	SUBROUTINE SUBSTT( PTR, LEN, EX, EY, CX, CY )
C
***	IDENT	15MR82
***	MODULE	SUBSTT
C
C created 15 March 1982
C dummy line pattern SUBSTiTution routine for WOSPLIB
C Only here so we don't get an unresolved reference if SUBSTT
C is never given by the calling program (if incorrectly not
C given, then will only get blanks lines instead of patterned ones)
C
C doesn't do very much, really
C
	RETURN
C
	END
