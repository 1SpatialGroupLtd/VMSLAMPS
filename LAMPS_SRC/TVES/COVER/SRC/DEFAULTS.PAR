	INTEGER		DEFAULT_HEIGHT
	PARAMETER       (DEFAULT_HEIGHT = 0.0)

	INTEGER         DEFAULT_DIRECTION
	PARAMETER       (DEFAULT_DIRECTION = 0.0)

	INTEGER         DEFAULT_DISTANCE
	PARAMETER       (DEFAULT_DISTANCE = 0.0)

	INTEGER         DEFAULT_CONE
	PARAMETER       (DEFAULT_CONE = 360.0)
