C AC type for DFAD/DTED
C
	INTEGER		FADACC
	PARAMETER	(FADACC=8)
C
C bits in ARM are set if we want debug info output
C
	INTEGER*4	IFFLUN		! LUN for the IFF
	LOGICAL		HAD_LOG		! log option
	LOGICAL		HAD_MERGE	! .t. if merging IFF into DTI
	INTEGER*4	LPOSNO		! position of NO entry
	INTEGER*4	FAC		! current feature code
C
	COMMON /PARAMS/ LPOSNO, FAC, HAD_LOG, IFFLUN,
     &		        HAD_MERGE
C
